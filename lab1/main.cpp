// σκοπός είναι η εξάσκηση σε c. ο κώδικας θα γράφεται σε python και περιοδικά θα γίνονται ενημερώσεις στα .c αρχεία

#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <stdbool.h>
int networkSize;
bool infectionDone = false;
int currentInfected = 1;
int infectedPerLoop = 0;


int main() {
    printf("Input number of PCs on the network:");
    scanf("%d", &networkSize);
    int nodes [networkSize] = {0};
    while (infectionDone = false) {
        for (int i = 0; i<networkSize;i++) {
            
        }
    }
        